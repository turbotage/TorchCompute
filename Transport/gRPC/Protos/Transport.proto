syntax = "proto3";


service TransportService {
	// Sends a Tensor

	rpc TransportRPC(Data) returns (Data) {}

}

message Data {
	string id = 1;
	string data_path = 2;
	int32 data_type = 3;
	bytes data = 4;
	string meta = 5;
}